{
  "name": "effect-bdd-benchmarks",
  "private": true,
  "type": "module",
  "scripts": {
    "bench": "pnpm run compare && pnpm run report",
    "build:compiled": "tsc -p tsconfig.build.json --noCheck",
    "check": "tsc -p tsconfig.json --noEmit",
    "smoke": "tsx src/compare.ts --iterations 1 --warmups 0 && pnpm run report",
    "pressure": "tsx src/compare.ts --iterations 1 --warmups 0 --generated-parse-features 200 --generated-parse-scenarios-per-feature 10 --generated-outline-examples 1000 --generated-discovery-features 200 --generated-discovery-scenarios-per-feature 5 --generated-parallel-scenarios 1000 --generated-reporter-scenarios 1000 --suite parse-scale --suite outline-scale --suite discovery-scale --suite parallel-scale --suite reporter-overhead && pnpm run report",
    "compare:compiled": "pnpm run build:compiled && tsx src/compare.ts --profile compiled",
    "compare": "tsx src/compare.ts",
    "report": "tsx src/report.ts"
  },
  "dependencies": {
    "effect": "4.0.0-beta.90",
    "effect-bdd": "link:.."
  },
  "devDependencies": {
    "@cucumber/cucumber": "^13.0.0",
    "@types/node": "^25.9.2",
    "tsx": "^4.21.0",
    "typescript": "^6.0.3"
  }
}
